<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed');

function figmaTop( object $object ) : int { 

    // Init
    $return = '';

    // Color
    $return = $object->y;


    return $return;
}